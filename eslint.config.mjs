import eslintConfig from '@antfu/eslint-config'

export default eslintConfig({
  typescript: true,
  vue: true,
  yaml: true,
  markdown: true,
  json: true,
  jsonc: true,
  json5: true,
  jsonld: true,
  formatters: true,
})
